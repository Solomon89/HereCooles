@model Hakaton_Db.Models.Point
<div class="taskModel">
    <div class="container">
        <div class="col-xs-span-2 col-xs-3">
            @Model.PointType.Name
        </div>
        <div class="col-xs-span-2 col-xs-3">
            @Model.Name
        </div>
        <div class="col-xs-span-2 col-xs-3">
            @Model.SubEvents.Count заданий
        </div>

    </div>
</div>
